# Local Platform Plugin

Dev/local messaging (SMS and email logging) and filesystem storage for development and single-node deployments.

Always enabled by default; hot-swappable with cloud storage or CPaaS messaging plugins in production.
